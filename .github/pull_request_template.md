**What this changes**

**Why**

**How it was checked**

- [ ] `cargo test --release` passes
- [ ] `cargo clippy --all-targets -- -D warnings` is clean
- [ ] `cargo fmt --check` is clean

If it changes a decision (when focus is taken back), say which row of the table in
the README it affects, and add the test that fails without the change.
